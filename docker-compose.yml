services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: prefect
      POSTGRES_PASSWORD: prefect
      POSTGRES_DB: prefect
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U prefect"]
      interval: 5s
      timeout: 5s
      retries: 10

  prefect-server:
    image: prefecthq/prefect:3.8.3-python3.14
    command: prefect server start --host 0.0.0.0
    ports:
      - "4200:4200"
    environment:
      PREFECT_API_DATABASE_CONNECTION_URL: postgresql+asyncpg://prefect:prefect@postgres:5432/prefect
      PREFECT_SERVER_API_HOST: 0.0.0.0
      PREFECT_UI_API_URL: http://localhost:4200/api
      PREFECT_SERVER_API_AUTH_STRING: ${PREFECT_AUTH_STRING:?Set PREFECT_AUTH_STRING in .env (see .env.example)}
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python3", "-c", "import base64, os, urllib.request; req = urllib.request.Request('http://localhost:4200/api/health'); req.add_header('Authorization', 'Basic ' + base64.b64encode(os.environ['PREFECT_SERVER_API_AUTH_STRING'].encode()).decode()); urllib.request.urlopen(req)"]
      interval: 5s
      timeout: 5s
      retries: 20

  runner:
    build: .
    environment:
      PREFECT_API_URL: http://prefect-server:4200/api
      PREFECT_API_AUTH_STRING: ${PREFECT_AUTH_STRING:?Set PREFECT_AUTH_STRING in .env (see .env.example)}
      PREFECT_LOGGING_LEVEL: DEBUG
      PREFECT_RUNNER_PROCESS_LIMIT: 3
    depends_on:
      prefect-server:
        condition: service_healthy

volumes:
  postgres-data:
